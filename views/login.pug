html
    head
        title Connexion
    body
        form.form-inline(action="/login", method="POST")
            div.form-group
                label(for="username") Nom d'utilisateur:
                input.form-control(name="username" placeholder="Nom d'utilisateur")
            div.form-group
                label(for="password") Mot de Passe:
                input.form-control(type='password' name="password" placeholder="Mot de passe")
            div
                button.btn-primary(type="submit") Connexion
    a(href="/users/register") Inscription